<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Parent on ProtonTheme (an AppCompat/Material descendant) so Proton Core's
         view-based auth activities that inherit the application theme render correctly. -->
    <style name="Theme.FrontWidget" parent="ProtonTheme" />

    <!-- Transparent theme for the headless launcher so no blank screen flashes
         behind the system permission dialog. -->
    <style name="Theme.FrontWidget.Transparent" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
